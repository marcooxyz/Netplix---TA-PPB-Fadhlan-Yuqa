# Netplix

This project contains a frontend and a backend.

## Frontend

The frontend is a React application located in the `frontend` directory. To run it, navigate to the `frontend` directory and run `npm install` and then `npm start`.

## Backend

The backend is a Node.js application located in the `backend` directory.
